// Property section---->>>>
var mapVisible = false;
function showMap() {
    var mapContainer = document.getElementById('mapContainer');
    mapVisible = !mapVisible; // Toggle the map visibility state
    if (mapVisible) {
        mapContainer.style.display = 'block'; // Show the map
    } else {
        mapContainer.style.display = 'none'; // Hide the map
    }
}
function showMap1() {
    var mapContainer = document.getElementById('mapContainer1');
    mapVisible = !mapVisible; // Toggle the map visibility state
    if (mapVisible) {
        mapContainer.style.display = 'block'; // Show the map
    } else {
        mapContainer.style.display = 'none'; // Hide the map
    }
}
function showMap2() {
    var mapContainer = document.getElementById('mapContainer2');
    mapVisible = !mapVisible; // Toggle the map visibility state
    if (mapVisible) {
        mapContainer.style.display = 'block'; // Show the map
    } else {
        mapContainer.style.display = 'none'; // Hide the map
    }
}
function showMap3() {
    var mapContainer = document.getElementById('mapContainer3');
    mapVisible = !mapVisible; // Toggle the map visibility state
    if (mapVisible) {
        mapContainer.style.display = 'block'; // Show the map
    } else {
        mapContainer.style.display = 'none'; // Hide the map
    }
}